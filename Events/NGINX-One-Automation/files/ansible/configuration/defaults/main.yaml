# Ansible Variables

nginx_one_f5xc_tenant: ""
nginx_one_f5xc_api_token : ""

# Use Ansible module or skip
state: "present"
#state: 'absent'

# Ansible Date Time
ansible_date_time: "{{ lookup('pipe', 'date +%Y-%m-%d.%H:%M:%S') }}"

# List of NGINX One Instance IDs
azure_instances: 
  - instance: inst_replace
  - instance: inst_replace